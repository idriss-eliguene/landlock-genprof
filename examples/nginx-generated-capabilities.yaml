# Illustrative example, adapted from README.md Step 4sexies's documented
# sample output — not a fresh live capture. See issue #94 for
# regenerating this from a real trace run.
#
# Would be generated by:
#   go run ./cmd/landlock-genprof trace --pod nginx-demo --namespace default \
#     --binary /usr/sbin/nginx --duration 60s --capabilities-out nginx-demo-capabilities.yaml
#
# Not a standalone applyable resource — paste this under a container's
# own securityContext.capabilities: key.

add:
  - NET_BIND_SERVICE   # confidence: high
drop:
  - ALL
