name: PodLock upstream real-node smoke

on:
  workflow_dispatch: {}
  push:
    branches: [feat/spo-merged-provenance]
    paths:
      - .github/workflows/podlock-upstream-smoke.yml
      - test/e2e/podlock-upstream-smoke.sh

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      KUBECONFIG: /etc/rancher/k3s/k3s.yaml
      K3S_VERSION: v1.33.6+k3s1
      PODLOCK_VERSION: 0.1.1
      CERT_MANAGER_VERSION: v1.18.2
      IMAGE: landlock-genprof/fsprobe:podlock-smoke
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v5
        with:
          version: v3.16.3
      - name: Install tools and pinned k3s
        run: |
          set -euo pipefail
          sudo apt-get update && sudo apt-get install -y gcc jq socat
          curl -sfL https://get.k3s.io | INSTALL_K3S_VERSION="$K3S_VERSION" \
            INSTALL_K3S_EXEC='server --write-kubeconfig-mode 644 --disable traefik --disable servicelb --disable metrics-server' sh -
          kubectl wait --for=condition=Ready node --all --timeout=300s
      - name: Install pinned cert-manager before PodLock
        run: |
          set -euo pipefail
          helm repo add jetstack https://charts.jetstack.io
          helm repo update jetstack
          helm install cert-manager jetstack/cert-manager --namespace cert-manager --create-namespace \
            --version "$CERT_MANAGER_VERSION" --set crds.enabled=true --wait --timeout 10m
          kubectl -n cert-manager rollout status deployment/cert-manager --timeout=300s
          kubectl -n cert-manager rollout status deployment/cert-manager-cainjector --timeout=300s
          kubectl -n cert-manager rollout status deployment/cert-manager-webhook --timeout=300s
      - name: Install pinned PodLock
        run: |
          set -euo pipefail
          helm repo add podlock https://flavio.github.io/podlock
          helm repo update podlock
          helm pull podlock/podlock --version "$PODLOCK_VERSION" --destination /tmp
          helm install podlock /tmp/podlock-${PODLOCK_VERSION}.tgz --namespace podlock --create-namespace --wait --timeout 10m
          kubectl rollout status deployment/podlock-controller -n podlock --timeout=300s
          kubectl rollout status daemonset/podlock-nri-plugin -n podlock --timeout=300s
          kubectl get crd landlockprofiles.podlock.kubewarden.io
          kubectl get mutatingwebhookconfiguration podlock-controller-webhook
          kubectl get validatingwebhookconfiguration podlock-controller-webhook-validating
      - name: Verify containerd NRI and build probe
        run: |
          set -euo pipefail
          kubectl get node -o jsonpath='{.items[0].status.nodeInfo.containerRuntimeVersion}' | tee runtime.txt
          grep -q '^containerd://' runtime.txt
          sudo k3s ctr plugins ls | tee containerd-plugins.txt
          grep -qi nri containerd-plugins.txt
          sudo test -S /var/run/nri/nri.sock
          mkdir -p /tmp/fsprobe-image
          gcc -static -O2 -Wall -Wextra test/e2e/landlock-fs-probe.c -o /tmp/fsprobe-image/fsprobe
          printf 'allowed-content\n' > /tmp/fsprobe-image/allowed.txt
          printf 'denied-content\n' > /tmp/fsprobe-image/denied.txt
          chmod 0555 /tmp/fsprobe-image/fsprobe
          chmod 0644 /tmp/fsprobe-image/allowed.txt /tmp/fsprobe-image/denied.txt
          docker build -f test/e2e/fsprobe-image.Dockerfile -t "$IMAGE" /tmp/fsprobe-image
          docker save "$IMAGE" | sudo k3s ctr images import -
      - name: Run upstream-equivalent PodLock smoke
        run: bash test/e2e/podlock-upstream-smoke.sh
      - name: Diagnostics
        if: always()
        run: |
          set +e
          mkdir -p diagnostics
          cp -R podlock-upstream-smoke-artifacts/. diagnostics/ 2>/dev/null
          kubectl get pods -A -o wide > diagnostics/pods.txt
          kubectl get events -A --sort-by=.lastTimestamp > diagnostics/events.txt
          kubectl -n podlock logs daemonset/podlock-nri-plugin -c nri --tail=5000 > diagnostics/nri.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: podlock-upstream-smoke
          path: diagnostics
