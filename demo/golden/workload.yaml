apiVersion: v1
kind: Pod
metadata:
  name: nginx-demo
  namespace: landlock-genprof-e2e
  labels:
    app: nginx-demo
spec:
  containers:
    - name: nginx
      image: nginx:1.25
      # keep as root so network bind/capability behavior is observable
      securityContext:
        runAsUser: 0
      ports:
        - containerPort: 80
