# Multi-cluster scenario

Select cluster A and its contexts, inspect scoped data, then select cluster B.
Identity, namespace, capabilities, and requests rebind; cluster-A data is
cleared rather than relabeled. Two tabs retain independent sessions and
namespace contexts.
