# Responsive behavior

The primary target is desktop/laptop. At 1440 and 1280 pixels the shell and
environment controls use a three-column grid. At 1024 they remain readable
without collision; technical tables may scroll deliberately. Narrower layouts
wrap controls and stack fields. Accidental horizontal overflow, clipped
primary actions, and ambiguous environment identity are defects.
