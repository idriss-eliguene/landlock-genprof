# M10.9-C Final Product Qualification

Date: 2026-09-21
Certified commit: `a597901a57b7f66b9cf3ab0af5564872a149a0c9`
Branch: `feat/operations-center-frontend-migration-v1`

## Environment

- `make env-doctor`: PASS
- Host: Darwin arm64; Go `go1.26.5`; Node `v22.19.0`; npm `10.9.3`
- Kubernetes: `v1.36.4`, context `kind-landlock-genprof-core`, runtime
  `containerd`
- Cilium: present; CoreDNS and project bootstrap: ready
- Required product CRDs: 6/6 present

## Qualification gates

- `go test ./...`: PASS
- `go test -race ./...`: PASS
- `go vet ./...`: PASS
- canonical envtest suite: PASS
- frontend typecheck: PASS
- frontend tests: PASS, 38 tests
- frontend production build: PASS
- route/security matrix: PASS
- real Playwright root/deep-link matrix: PASS at 1440, 1280, 1024, and 680
- `git diff --check`: PASS

The repository's older `ui-lima-auth-test` smoke driver still waits for
legacy `[data-view=...]` selectors and therefore is not a valid final React
smoke oracle. It timed out before exercising the current shell. The canonical
root was qualified with the current `migration-app` marker and real browser
navigation instead; no product source change was made for the stale driver.

## Certified invariants

- `/` is the React Operations Center; React deep links hard-refresh correctly.
- Unsafe root methods return 405.
- API and health routes take precedence over SPA fallback.
- Existing assets are served; missing assets return 404; `/next/*` returns 410.
- Trusted-proxy checks: unsigned 401, valid signed request 200, stale signed
  request 401.
- Explicit-only mode does not enumerate namespaces; context transitions and
  multitab isolation remain qualified.
- P3.1 remains: one operations-context projection, 13 SSARs, no double
  projection, false executor loss 0, unexpected 502s 0.
- B1 empirical scale evidence: 1000 Observations and 1000 Proposals each
  retrieved in 10 pages with 1000 unique items, zero duplicates, zero missing,
  and terminal `complete=true`.
- Proposal delete/recreate remains UID-safe: old UID request 409, no rebind to
  the replacement UID, and UID-partitioned frontend detail identity.
- Workload lineage remains exact; mixed and insufficient provenance are not
  promoted to ownership.
- Approved is not Applied; Applied is not structurally or behaviorally
  verified; behavioral verification remains UNKNOWN without proof.
- Health remains a non-scored dimension ledger. Zero observations remain
  `NOT_ESTABLISHED`; malformed diagnostics remain visible; evaluation time is
  not freshness.
- Reconciliation Attention and SPHM Attention remain separate taxonomies.
- History remains a bounded projection, not a complete transactional global
  audit log.
- Legacy Workbench rendering, templates, script runtime, and public entrypoint
  are removed; `/next/*` is retired.

## Known limitations

- Candidate YAML is a derived non-authoritative engineering view, not full
  Kubernetes SecurityProfileProposal YAML or governance identity.
- Apply does not establish behavioral verification.
- Observation/collection/qualification timestamps are unavailable where the
  authoritative API does not expose them; projection evaluation time is not
  freshness.
- Activity/History is not a complete transactional global event ledger.
- The two Attention projections are not one canonical incident model.

No domain, CRD, RBAC, trusted-proxy, governance, digest, SPHM, polling,
QPS/Burst, or WriteTimeout semantics changed during final qualification.
